# Redis is configured but unreachable — proves the fan-out (D4) degrades
# gracefully: BuildRedisFromEnv returns null, both cache and logger skip
# their "redis" layer/handler without throwing.
REDIS_HOST=nonexistent_host_that_does_not_exist
REDIS_PORT=6379

CACHE_LAYERS=redis
LOG_HANDLERS=redis
